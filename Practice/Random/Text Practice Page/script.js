function changeColor(property)
{
    text = document.getElementById(property);

    text.style["color"] = "#" + Math.floor(Math.random() * 1000000);
}

function displayTwoLinks(property)
{
    if(Math.floor(Math.random() * 2) == 1);

}